import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function HomeScreen() {

  return (
    <SafeAreaView>
    <View>
      <Text style={{ color: '#e70606ff'}}>Hej bajs</Text>
    </View>

    </SafeAreaView>

  );
}